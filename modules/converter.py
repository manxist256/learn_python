# Modules is a single .py file
# Functions in modules are written without enclosing fn.s inside a class
# Group similar functions and classes and write it inside a module

def lbs_to_kg(weight):
    return weight * 0.45
